# 💕 我们的恋爱小屋（Supabase 版）详细操作手册

> 目标读者：不要求会写代码，照着做就能用。
> 全程不需要手机号注册 App，也不需要自己买服务器。

---

## 目录
1. 一页速览（最快跑起来）
2. 准备工作
3. 第一步：注册并创建 Supabase 项目
4. 第二步：执行建表脚本 setup.sql
5. 第三步：填写配置 js/config.js
6. 第四步：本地启动测试
7. 两个人怎么一起用（核心流程）
8. 各功能模块说明
9. 上线部署（手机随时打开）
10. 常见问题排查
11. 安全须知
12. 数据清理与备份

---

## 1. 一页速览（最快跑起来）

只要做 4 件事：

1. 注册 Supabase，创建一个项目。
2. 把项目里的 `setup.sql` 内容粘到 Supabase 的 SQL Editor 里执行。
3. 把 Supabase 的 `Project URL` 和 `anon public key` 填进 `js/config.js`。
4. 用本地服务器打开网页（不要双击 file:// 打开）。

做完就能创建房间、两个人实时同步了。

---

## 2. 准备工作

你需要：

| 东西 | 说明 |
|---|---|
| 一个邮箱 | 用来注册 Supabase 账号 |
| 能上网的电脑 | 首次配置在电脑上操作更方便 |
| 本项目文件夹 | `couple-love-supabase` 整个文件夹 |

> 国内提示：如果打开 Supabase 官网很慢，可以用国产兼容平台 **MemFire Cloud**（API 完全兼容，SDK 通用），操作几乎一样，后面会单独说明。

---

## 3. 第一步：注册并创建 Supabase 项目

1. 打开浏览器，访问：https://supabase.com/dashboard
2. 点右上角 **Sign in / 登录**，用 GitHub 或邮箱注册登录。
3. 登录后点 **New project**（新建项目）。
4. 填写：
   - **Name**：随便起，例如 `love-house`
   - **Database Password**：设置一个数据库密码（**务必记住**，以后可能要用）
   - **Region**：选离你近的地域，例如 `Southeast Asia (Singapore)` 或 `Northeast Asia (Tokyo)`
5. 点 **Create new project**，等待 1～2 分钟创建完成。

创建完成后，你会进入项目控制台，左侧有一列菜单：
`Table Editor / SQL Editor / Storage / Authentication / Project Settings` 等。

---

## 4. 第二步：执行建表脚本 setup.sql

这一步会在云端自动建好 6 张数据表和图片存储桶。

1. 在左侧菜单点 **SQL Editor**。
2. 点 **New query**（新建查询）。
3. 用记事本/编辑器打开项目里的 `setup.sql` 文件，**全选复制**所有内容。
4. 粘到 SQL Editor 的输入框里。
5. 点右下角 **Run**（或按 Ctrl+Enter）。

### 怎么确认成功？

- 左侧点 **Table Editor**：应该能看到 6 张表：
  `love_rooms、love_members、love_diary、love_anniversaries、love_photos、love_messages`
- 左侧点 **Storage**：应该能看到一个桶 `love_photos`（Public 公开）。

> 如果没看到，回到 SQL Editor 重新 Run 一次（这个脚本可以重复执行，不会出错）。

---

## 5. 第三步：填写配置 js/config.js

1. 左侧点 **Project Settings**（齿轮图标）。
2. 点 **API**。
3. 复制两样东西：
   - **Project URL**（形如 `https://xxxxxxx.supabase.co`）
   - **anon public**（一串很长的字符，形如 `eyJhbGciOi...`）
4. 用记事本打开项目里的 `js/config.js`，找到这一段：

```js
SUPABASE: {
  projectUrl: "YOUR_PROJECT_URL",
  anonKey: "YOUR_ANON_KEY"
},
```

5. 把 `YOUR_PROJECT_URL` 换成你的 Project URL，把 `YOUR_ANON_KEY` 换成 anon public key，保存文件。

⚠️ 注意：要用的是 **anon public**，不是 `service_role`。`service_role` 是服务端密钥，绝对不能放进网页。

---

## 6. 第四步：本地启动测试

⚠️ 重要：**不能直接双击 index.html 打开**（file:// 协议下实时同步会失败），必须用本地 HTTP 服务器。

### 方法 A：用 Python（推荐）

1. 电脑已装 Python 的话，打开项目文件夹。
2. 在文件夹地址栏输入 `cmd` 回车（或用终端进入该目录）。
3. 输入：

```
python -m http.server 8080
```

4. 打开浏览器访问：http://localhost:8080

### 方法 B：用 Node.js

```
npx serve .
```

然后访问提示的地址（一般是 http://localhost:3000）。

### 如何用手机一起测试？

- 电脑开好本地服务器后，确保**手机和电脑在同一个 WiFi**。
- 查电脑局域网 IP（Windows：`ipconfig` 里的 IPv4 地址，例如 `192.168.1.5`）。
- 手机浏览器访问：`http://192.168.1.5:8080`

这样就能电脑 + 手机两台设备同时测实时同步了。

---

## 7. 两个人怎么一起用（核心流程）

### 第一个人（创建房间）

1. 打开网页，看到「✨ 创建房间 / 🔑 加入房间」。
2. 选 **创建房间**。
3. 填自己的昵称（如「小桃」）。
4. 选「在一起的日子」。
5. 点 **创建我们的房间**。
6. 页面会弹出一个 **6 位数字密钥**（如 `123456`）。
7. 把这个密钥发给对方（微信/当面都行）。

### 第二个人（加入房间）

1. 在另一台设备打开同一个网址。
2. 选 **加入房间**。
3. 填自己的昵称（如「小满」）。
4. 输入对方给的 6 位密钥。
5. 点 **加入 TA 的房间**。

进入后，两个人看到的是同一份数据。

### 验证实时同步

- 一方在「日记」写一条，另一方页面**不用刷新**，会自动出现。
- 一方上传照片、加纪念日、留言，另一方都会自动更新。

---

## 8. 各功能模块说明

| 模块 | 怎么用 |
|---|---|
| 🏡 首页 | 看相恋天数、欢迎语、最近的纪念日/日记/照片/留言；点情话可换一句 |
| ⏳ 倒计时 | 看在一起的总时长（每秒跳动）+ 各纪念日还有几天 |
| 📖 日记 | 选心情、写正文，点发布；双方实时可见 |
| 📷 相册 | 选图（自动压缩）→ 点上传；点照片可放大查看 |
| 🎀 纪念日 | 添加纪念日名称和日期；可编辑、删除，自动算还有几天 |
| 💌 留言板 | 给 TA 留言；勾选「匿名」可匿名发送 |
| ⚙️ 设置 | 改昵称、改在一起的日子、复制房间密钥、退出房间 |

---

## 9. 上线部署（手机随时打开）

本地服务器只有你自己电脑开着才能访问，想让两个人随时随地用，就把它发布到网上。

### 方案 1：Vercel（推荐，免费，最省事）

1. 打开 https://vercel.com ，注册登录。
2. 点 **Add New → Project**。
3. 把 `couple-love-supabase` 文件夹拖进去，或连接 GitHub 仓库。
4. 框架选 **Other**（纯静态），点 **Deploy**。
5. 部署完成后得到一个 `https://xxx.vercel.app` 网址，双方手机/电脑打开即可。

### 方案 2：Netlify

1. 打开 https://app.netlify.com ，注册登录。
2. 把 `couple-love-supabase` 文件夹拖进 Netlify 页面。
3. 自动生成一个 `https://xxx.netlify.app` 网址。

### 方案 3：GitHub Pages / Gitee Pages

1. 把项目推送到 GitHub / Gitee 仓库。
2. 开启 Pages 服务，发布分支后得到网址。

### 方案 4：国内静态托管（腾讯云 COS / 阿里云 OSS）

1. 开通对象存储，创建存储桶，开启「静态网站托管」。
2. 把 `couple-love-supabase` 里的文件（index.html、css、js）上传到桶根目录。
3. 用提供的访问域名访问。

### 部署后（建议做）

把部署得到的 `https://` 域名填到 Supabase：
`Authentication → URL Configuration → Site URL`，保存。这一步不是必做，但填了更稳。

---

## 10. 常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 页面提示「还没有配置 Supabase」 | config.js 还是占位符 | 重新填 projectUrl / anonKey |
| 创建房间失败/没反应 | setup.sql 没执行 | 回 SQL Editor 重新 Run |
| 能写数据，但对方不自动刷新 | Realtime 没开启 | 重新执行 setup.sql（第 4 段） |
| 报 `permission denied` | RLS 策略缺失 | 重新执行 setup.sql（RLS 部分） |
| 图片上传失败 | 没建 love_photos 桶 | 重新执行 setup.sql（第 5 段） |
| 页面样式乱 | Tailwind CDN 没加载 | 换网络，或把 Tailwind 下载到本地 |
| file:// 打开没数据 | 用了双击打开 | 改用本地 http 服务器 |
| 手机连不上电脑 | 不在同一 WiFi 或 IP 填错 | 检查 WiFi 和局域网 IP |

---

## 11. 安全须知

1. ⚠️ **6 位房间密钥 = 权限凭证**：知道密钥的人就能加入房间并看到/修改数据。只私发给 TA，不要发到公开群、朋友圈。
2. **anonKey 可以公开**（放在网页里没关系），但 `service_role key` 绝不能放进前端。
3. 本项目为了做到「免注册」，RLS 采用匿名可读写。这是情侣个人使用的合理选择；若要做公开产品，应接入 Supabase 账号体系并收紧权限。

---

## 12. 数据清理与备份

### 删除某条数据

在 App 里直接删除即可（日记目前只展示，纪念日可删除；照片和留言可删除）。更多删除操作可在 Supabase **Table Editor** 里选中行删除。

### 删除整个房间

在 Supabase **Table Editor**：
- 打开 `love_rooms`，删除对应 `room_code` 的那一行（级联会删掉该房间的所有成员、日记、照片、纪念日、留言）。
- 图片文件可到 **Storage → love_photos** 里手动清理。

### 备份

Supabase 有自动备份（免费版有周期备份）；也可在 **Database → Backups** 查看。个人重要照片建议再本地留存一份。

### 免费额度提示

Supabase 免费套餐对两个人的日记、照片、留言完全够用。免费项目若长时间不活动可能被自动暂停，届时进控制台点 **Restore project** 即可恢复。

---

## 附：国内替代方案 MemFire Cloud（可选）

如果 Supabase 官网或接口在国内访问慢，可换国产兼容平台 **MemFire Cloud**：

1. 打开 https://memfiredb.com 注册登录，创建一个应用（免费）。
2. 在应用设置里拿到 `Project URL` 和 `anon public key`。
3. 把这两项填进 `js/config.js` 即可（代码不用改，SDK 完全兼容）。
4. SQL 脚本同样在 MemFire 的 SQL 编辑器里执行。

其余步骤与本手册完全一致。